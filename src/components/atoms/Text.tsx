import React from "react"

type Props = {}

const text = (props: Props) => {
  return <div>text</div>
}

export default text

// link button type > anchor tag > 밑줄이 생김
// function button type > button tag > cursor: pointer 가 됨

// 공통적으로 굵어지기도 함
// 다양한 text 사이즈가 있음

// 단순 heading 용도 있다 > 위젯보드
